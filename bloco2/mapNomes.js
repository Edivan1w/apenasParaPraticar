const nomes = ['lucas', 'pedro', 'paulo']

const nomesAtualizados = nomes.map(nome => nome.toUpperCase())
console.log(nomesAtualizados)

const nA = nomesAtualizados.map( nome => nome.toLowerCase())
console.log(nA)